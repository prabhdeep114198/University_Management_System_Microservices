server:
  port: 8084

spring:
  application:
    name: faculty-service
  datasource:
    url: jdbc:mysql://mysql-faculty-service:3306/ums_facultyservice_db?createDatabaseIfNotExist=true
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    hostname: faculty-service

