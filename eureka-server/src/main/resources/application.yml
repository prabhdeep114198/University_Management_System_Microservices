server:
  port: 8761

eureka:
  client:
    register-with-eureka: false      # Standalone server does not register with itself
    fetch-registry: false            # Standalone server does not fetch registry
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

